# Application Name
spring.application.name=spring-boot-backend

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/scm
spring.datasource.username=root
spring.datasource.password=chicken65@!!!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect


spring.jpa.hibernate.ddl-auto=update

# Optional: Show SQL queries in logs
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# Ethereum configuration

ethereum.node-url=http://localhost:8545  
ethereum.contract.address=
ethereum.privateKey=
logging.level.org.springframework=TRACE
#logging.level.org.web3j=OFF  # Turn off Web3j logging

# Enable Web3j only in production profile
spring.profiles.active=dev  # Default to dev profile
liquibase.change-log=classpath:/db/changelog/your-changelog-file.yaml
spring.liquibase.enabled=false


#debug=true
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl